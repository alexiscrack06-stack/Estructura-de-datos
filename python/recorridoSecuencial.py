# Declaración del array (lista)
numeros = [10, 20, 30, 40, 50]

# Recorrido por elementos
for num in numeros:
    print(f"Elemento: {num}")

# Recorrido por índice
for i in range(len(numeros)):
    print(f"Índice {i}: {numeros[i]}")